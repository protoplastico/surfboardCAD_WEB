**[解決済み 2026-08-20]** この申し送りが書かれた後の同日中に、ロッカー適用処理の
タンジェント処理を修正する4コミット（`619a338`〜`ea858a8`）が入り、中央接合部の
V字化・厚み不整合は解消済み。tailKick/entryLift付きプリセット（申し送りが示唆する
経路）を再現した診断で、両端の厚みが不連続なく滑らかに0へテーパーすることを確認。
再発防止のリグレッションテストを `test-core.js` の rocker セクションに追加した
（"tailKick/entryLift ... near-duplicate knots" / "... taper smoothly near x="）。
以下は当時の記録として残す。

# GPT-5.6-Sol 申し送り

## 対象

- プロジェクト: `ドキュメント/Projects/BoardCAD-Web-Linux`
- 確認対象: `デスクトップ/ラウンドピンテール.brd`
- 起動: `http://localhost:8788/`
- 次の起動コマンド: `codex -m gpt-5.6-sol`

## これまでの症状

- ロッカー補正後、ボード中央の接合部が逆への字状に見える。
- 中央に制御点が集中する状態がある。
- ボトムとデッキの接合部で厚みが合わず、両端が実際より薄く見える。
- 真平らなボードをサンプルにして、中央接合と厚みの計算を切り分ける方針。

## 今回のセッションで行ったこと

一時的に以下を実装・検証したが、最終的に今回のセッションで加えた変更は元に戻した。

- デッキ／ボトム端点の自動接合
- 平坦ロッカーの頂点を中央へ寄せる処理
- ベジェの x→t 変換を二分法にする処理
- 中央フラット区間の接線を水平に揃える処理
- ロッカー差分をデッキへ適用する厚み補正
- 平坦ボードの診断テストと数値ログ
- `ラウンドピンテール.brd` のローカルサンプル登録

ユーザーの指示により、上記の一時変更はすべて撤回した。コピーしていた `ラウンドピンテール.brd` も配信フォルダから削除し、元ファイルは `デスクトップ` 側に残している。

## 現在の状態

- `app.js`、`index.html`、`test-core.js` は今回のセッション開始時の状態へ戻した。
- `index.html` のアプリ版番号は元の `20260710-rail-fix`。
- 診断用の `BOARDCAD_DIAGNOSTIC`、平坦ボード用テスト、端点ギャップ用テストは残していない。
- ユーザー側の既存変更は意図的に保持した。

## 実行済みテスト

以下は撤回前および復元後に実行済み。

```bash
node --check app.js
node --check test-core.js
node test-core.js --section=rocker
node test-core.js
```

復元後も構文チェックと全体テストは成功した。

## 次に行うべき検証

1. 真平らなボトムとデッキを持つ最小 `.brd` サンプルを作る。
2. 補正前後で、中央接合部の以下を比較する。
   - 制御点の x/y
   - `prev` / `next` ハンドル角度
   - ボトム値、デッキ値、厚み
3. ノーズ・テール・中央の同一 x 位置で厚みを比較する。
4. ベジェ評価値と直接的なベジェ曲線サンプル値を比較する。
5. 問題がベジェ評価器ではなく、ロッカー補正と厚み補正のどちらにあるかを確定してから、最小の修正だけを入れる。

## 注意

補正を重ねて見た目を合わせるのではなく、元のボトム／デッキ曲線、変位量、厚みの定義を整理してから修正すること。特に端点の強制接合は、意図せず両端の厚みをゼロへ近づける可能性がある。
